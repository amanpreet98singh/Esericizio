import { GenereDirective } from './genere.directive';

describe('GenereDirective', () => {
  it('should create an instance', () => {
    const directive = new GenereDirective();
    expect(directive).toBeTruthy();
  });
});
